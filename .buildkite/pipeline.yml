agents:
  image: docker.elastic.co/ci-agent-images/cloud-k8s-operator/buildkite-agent:26b287eb
  cpu: "4"
  memory: "2G"

env:
  VAULT_ROOT_PATH: secret/ci/elastic-cloud-on-k8s

steps:

  - command: buildkite-agent pipeline upload .buildkite/notify-a.yml

  - command: buildkite-agent pipeline upload .buildkite/notify-b.yml

  - command: buildkite-agent pipeline upload .buildkite/notify-c.yml

  - command: buildkite-agent pipeline upload .buildkite/notify-70.yml
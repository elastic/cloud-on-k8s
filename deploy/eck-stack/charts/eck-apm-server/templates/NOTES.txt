
1. Check ApmServer status
  $ kubectl get apmserver {{ include "apm-server.fullname" . }} -n {{ .Release.Namespace }}

2. Check ApmServer pod status
  $ kubectl get pods --namespace={{ .Release.Namespace }} -l apmserver.k8s.elastic.co/name={{ include "apm-server.fullname" . }}

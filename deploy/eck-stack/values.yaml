---
# Default values for eck-stack.
# This is a YAML-formatted file.

# If enabled, will use the eck-elasticsearch chart and deploy an Elasticsearch cluster.
#
eck-elasticsearch:
  enabled: true

# If enabled, will use the eck-kibana chart and deploy a Kibana instance.
#
eck-kibana:
  enabled: false

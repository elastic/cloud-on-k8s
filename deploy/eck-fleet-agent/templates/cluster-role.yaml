{{- if .Values.clusterRole }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Values.clusterRole.name }}
  labels:
    {{- include "fleet-agent.labels" . | nindent 4 }}
rules: {{- toYaml .Values.clusterRole.rules | nindent 2 }}
{{- end }}
apiVersion: kibana.k8s.elastic.co/v1alpha1
kind: Kibana
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: kibana-sample
spec:
  version: "6.4.2"

  featureFlags:
    nodeCertificates:
      enabled: true

  nodeCount: 1
  expose: LoadBalancer

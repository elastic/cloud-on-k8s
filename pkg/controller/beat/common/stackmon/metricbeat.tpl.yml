metricbeat.modules:
- module: beat
  metricsets:
    - stats
    - state
  period: 10s
  hosts: ["{{ .URL }}"]
  xpack.enabled: true
{{if .ClusterUUID }}
monitoring.cluster_uuid: "{{ .ClusterUUID }}"
{{end}}
# Elasticsearch output configuration is generated and added here
